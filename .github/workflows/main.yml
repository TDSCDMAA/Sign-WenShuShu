name: main

on:
    workflow_dispatch:
    schedule:
        - cron: "20 1,6 * * *"
    watch:
        types: [started]

jobs:
    build:
        runs-on: windows-latest
        if: github.event.repository.owner.id == github.event.sender.id || ! github.event.sender.id
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: "Set up Python"
              uses: actions/setup-python@v5
              with:
                python-version: '3.10'

            - name: "安装依赖包"
              run: |
                  pip install -r ./requirements.txt

            - name: "运行 文叔叔签到"
              run: python Sign_WenShuShu_selenium.py
              env:
                  USER: ${{ secrets.USER }}
                  PASSWORD: ${{ secrets.PASSWORD }}
                  PUSH_MESSAGE: ${{ secrets.PUSH_MESSAGE }}
                  SHOW_USER: ${{ secrets.SHOW_USER }}
                  

            - name: "生成保活时间戳"
              run: |
                  echo "# Auto Keepalive - $(date)" > keepalive.txt
                  echo "Last updated: $(date -R)" >> keepalive.txt
                  echo "Workflow: ${{ github.workflow }}" >> keepalive.txt
                  
            - name: "配置 Git 用户"
              run: |
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"
                  
            - name: "提交保活更改"
              run: |
                  git add keepalive.txt
                  git commit -m "🔧 Auto keepalive: $(date +'%Y-%m-%d %H:%M')" || exit 0
                  git push